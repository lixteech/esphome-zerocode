{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist",
  "env": {
    "NPM_CONFIG_LEGACY_PEER_DEPS": "true",
    "NPM_CONFIG_FORCE": "true"
  },
  "rewrites": [
    {
      "source": "/esphome-zerocode/(.*)",
      "destination": "/esphome-zerocode/index.html"
    }
  ]
}
